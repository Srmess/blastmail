import CampaignLayout from '@/components/campaigns/tabs/layout';
import { OpenTabContent } from '@/components/campaigns/tabs/open';

export default function Open() {
    return (
        <CampaignLayout>
            <OpenTabContent />
        </CampaignLayout>
    );
}
